databases:
  - name: postgres
    ipAllowList: []
    databaseName: budget_manager_aagy
    user: budget_manager_aagy_user

services:
  - type: web
    name: budget_manager
    env: ruby
    buildCommand: "./render-build.sh"
    startCommand: "bundle exec rails s"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: postgres
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false