<div class="p-2 mt-4">
  <div class="row justify-content-center">
    <div class="col-md-12">
      <div class="card">
        <div class="card-header">
          Edit Entity
        </div>
        <%= form_for([@group, @entity]) do |f| %>
          <%= render 'form', entity: @entity, f: f %>
        <% end %>
      </div>
    </div>
  </div>
</div>
